# ============================================================
# ResumeAI Pro — requirements.txt
# Install: pip install -r requirements.txt
# ============================================================

# Core web framework
flask>=2.3.0
flask-cors>=4.0.0

# PDF parsing (try pypdf first, fallback to PyPDF2)
pypdf>=3.0.0
PyPDF2>=3.0.0           # fallback

# DOCX support
python-docx>=1.0.0

# NLP / ML
scikit-learn>=1.3.0
numpy>=1.24.0

# PDF generation (resume download)
reportlab>=4.0.0

# Anthropic Python SDK (for server-side /ai_enhance endpoint)
# Set ANTHROPIC_API_KEY env var to enable.
# The browser-side AI Enhance feature calls the API directly — no server key needed.
anthropic>=0.25.0

# ---- Optional / Dev ----
# openai>=1.0.0          # legacy OpenAI support (disabled by default)
# gunicorn>=21.0.0       # production WSGI server

# ============================================================
# Quick start:
#   python fix_db.py          # initialize SQLite schema
#   python app.py             # run dev server on :5000
#   Open index.html in browser or serve with:
#   python -m http.server 8080
# ============================================================
